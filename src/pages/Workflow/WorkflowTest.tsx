import Workflow from '../Workflow';

const WorkflowTest = () => {
  return (
    <Workflow
      category="test"
      title="Тестовые флоу"
      description="Воркфлоу для тестирования и экспериментов."
    />
  );
};

export default WorkflowTest; 